# Required for Grav's .htaccess rewrite rules to work.
# Without AllowOverride All, Apache ignores .htaccess entirely and PHP is never invoked.
<Directory /var/www/html>
    AllowOverride All
    Options -Indexes +FollowSymLinks +MultiViews
    Require all granted
</Directory>
